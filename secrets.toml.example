# Copy this to .streamlit/secrets.toml and fill in your key.
# For Railway/production: set these as environment variables in the dashboard instead.
# NEVER commit a real key to git.

# OPENROUTER_API_KEY = "sk-or-v1-..."
